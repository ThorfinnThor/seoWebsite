import Link from "next/link";
import { Breadcrumbs } from "./Breadcrumbs";

export interface GuideSection { title: string; paragraphs: string[]; bullets?: string[] }

export function GuidePage({ title, intro, updated, sections, takeaway }: { title: string; intro: string; updated: string; sections: GuideSection[]; takeaway: string }) {
  return <><article className="guide-page"><Breadcrumbs items={[{ label: "Start", href: "/" }, { label: "Garten", href: "/garten/" }, { label: title }]} /><header><p className="eyebrow">MachPlan Ratgeber · zuletzt geprüft {updated}</p><h1>{title}</h1><p>{intro}</p></header><div className="guide-layout"><aside><strong>Direkt zum Ergebnis</strong><p>{takeaway}</p><Link className="button button--primary" href="/garten/gartenhaus-planer/">Gartenhaus planen →</Link></aside><div className="guide-copy">{sections.map((section) => <section key={section.title}><h2>{section.title}</h2>{section.paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}{section.bullets && <ul>{section.bullets.map((bullet) => <li key={bullet}>{bullet}</li>)}</ul>}</section>)}<div className="guide-limit"><strong>Wichtige Grenze</strong><p>Diese Einordnung ersetzt keine Prüfung von Baurecht, Abständen, Statik, Fundament oder Herstellerangaben für deinen konkreten Standort.</p></div></div></div></article><section className="guide-cta"><div><p className="eyebrow">Vom Wissen zur Auswahl</p><h2>Berechne jetzt deinen Planungsrahmen.</h2><p>Der Planer verbindet deinen Lagerbedarf mit Stellfläche, Türbreite, Bodenwunsch und Budget.</p></div><Link className="button button--light" href="/garten/gartenhaus-planer/">Planer kostenlos starten →</Link></section></>;
}
